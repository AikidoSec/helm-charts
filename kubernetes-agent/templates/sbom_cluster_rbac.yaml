apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ .Values.sbomCollector.name | quote }}
rules:
  # Core ("" group)
  - apiGroups: [""]
    resources:
      - pods
    verbs: ["get","list","watch"]
  - apiGroups: [ "" ]
    resources:
      - serviceaccounts
    verbs: [ "get" ]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ .Values.sbomCollector.name | quote }}
subjects:
  - kind: ServiceAccount
    name: {{ .Values.sbomCollector.name | quote }}
    namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: {{ .Values.sbomCollector.name | quote }}
  apiGroup: rbac.authorization.k8s.io
