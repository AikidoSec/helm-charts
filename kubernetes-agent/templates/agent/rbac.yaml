apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "kubernetes-agent.fullname" . }}
  namespace: {{ .Release.Namespace }}
rules:
  - apiGroups: [""]
    resources: ["secrets"]
    verbs: ["update","patch","get"]
    resourceNames:
      - {{ include "kubernetes-agent.secretName" . }}
  - apiGroups: ["apps"]
    resources: ["deployments", "daemonsets"]
    verbs: ["update","patch"]
    resourceNames:
      # The agent needs to be able to restart itself when ignore namespaces are updated
      - {{ include "kubernetes-agent.fullname" . }}
      # The agent needs to be able to update the replica count/node selector of the collector
      - {{ include "sbom-collector.name" . }}
      - {{ include "tdr.name" . }}
  - apiGroups: [ "" ]
    resources: [ "configmaps" ]
    verbs: [ "update","patch" ]
  - apiGroups: [ "metrics.k8s.io" ]
    resources: [ "pods" ]
    verbs: [ "get", "list" ]
  - apiGroups: [ "" ]
    resources: [ "events" ]
    verbs: [ "get", "list" ]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "kubernetes-agent.fullname" . }}
  namespace: {{ .Release.Namespace }}
subjects:
  - kind: ServiceAccount
    name: {{ include "kubernetes-agent.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "kubernetes-agent.fullname" . }}
